{
  "dependencies": {
    "@google/genai": "^1.44.0",
    "multer": "^2.1.0",
    "razorpay": "^2.9.6"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
